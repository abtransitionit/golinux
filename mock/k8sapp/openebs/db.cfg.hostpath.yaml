# Enable all LocalPV engines
engines:
  local:
    lvm: { enabled: false }
    zfs: { enabled: false }
  replicated:
    mayastor: { enabled: false }


# needed to have correct label on nede where provisionner is provisioned
openebs-ndm: { enabled: true }

monitoring: { enabled: false }

loki: { enabled: false }
minio: { enabled: false }
alloy: { enabled: false }

helper: { enabled: false }
