
# Disable unwanted engines
engines:
  replicated:
    mayastor:
      enabled: false

  local:
    lvm:
      enabled: false
    zfs:
      enabled: false

# Optional but explicit
ndm:
  enabled: true
